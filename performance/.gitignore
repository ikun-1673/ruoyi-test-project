# ============================================================
# 本地产物 / 不适合作品集的文件（不随仓库提交）
# 其余文件（文档、脚本、图表、原始数据）均应提交
# ============================================================

# JMeter 自动生成的 HTML 报告（17MB，属生成物，可用 README「五、如何复现」一行命令重生成）
reports/

# 个人操作笔记（非交付物）
P3实操手册.md

# 含本机部署路径的应急脚本（路径写死了本机环境，他人无法复用）
scripts/restart_backend.bat

# Python 缓存
__pycache__/
scripts/__pycache__/
*.pyc

# JMeter 运行日志（每次运行自动生成）
jmeter.log
